# 开发环境配置

# ========== 切换说明 ==========
# 公网演示：使用 cpolar 地址（当前启用）
# 本地开发：使用 localhost 地址
# 切换时只需注释/取消注释对应行，然后重启 npm run dev
# ==============================

# 公网演示时使用（当前启用）
VITE_API_BASE_URL=https://116dd4ff.r16.cpolar.top/api/v1

# 本地开发时使用（公网演示时注释掉）
# VITE_API_BASE_URL=http://localhost:8080/api/v1
